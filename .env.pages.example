# Used by `npm run build:pages` and `npm run deploy`
# Copy to .env.pages and set your hash before deploying to GitHub Pages.
#
# Generate a hash:
#   npm run password:hash -- your-password
#
# VITE_SITE_ACCESS_HASH=your_sha256_hash_here
